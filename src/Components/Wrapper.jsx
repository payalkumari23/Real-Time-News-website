import React from 'react'

const Wrapper = ({children}) => {

  return (
    <div className="max-w-7xl mx-auto px-4 m-auto items-center justify-center">
      {children}
    </div>
  )
}

export default Wrapper
